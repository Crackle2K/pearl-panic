# AI Agent Instructions

This repository is a small Python game project.

- Language: Python
- Framework: `pygame` (game logic expected in `src/`)
- Key files: `src/main.py`, `src/levels.py`, `src/sprites.py`, `src/data.py`

## How to work in this repo

- Keep changes small and focused on game behavior, structure, and correctness.
- Prefer plain Python and simple object-oriented design.
- Do not introduce machine learning or AI-specific libraries, packages, or external APIs.
- There are no existing AI features in the codebase; do not add AI features unless explicitly requested.
- Avoid copying large blocks of unrelated code; keep edits minimal and idiomatic.

## If extending the project

- Use `pygame` for game functionality.
- Keep file structure simple and consistent with the existing `src/` layout.
- Document behavior only when it helps clarify game logic.

## Notes

- The repository currently has no README or tests.
- Follow the user’s explicit directive: disable or avoid AI-related functionality.
